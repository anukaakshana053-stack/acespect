{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"./node_modules/.bin/prisma migrate deploy && node dist/scripts/seed-prepurchase-apartment.js && node dist/scripts/seed-prepurchase-house-commercial.js && exec node dist/server.js\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
