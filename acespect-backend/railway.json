{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"./node_modules/.bin/prisma migrate resolve --rolled-back 20260715110125_add_template_profile_dimension; ./node_modules/.bin/prisma migrate deploy && node dist/scripts/seed-templates.js && exec node dist/server.js\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
